# Minimal dependencies for CI (tests + lint + typecheck)
# Avoid heavy geospatial stack to keep CI lightweight.

# Local monorepo package (provides the `skyulf` module)
-e ./skyulf-core

fastapi>=0.104.0,<1.0.0
uvicorn>=0.24.0,<1.0.0
pydantic>=2.4.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
email-validator>=2.0.0
sqlalchemy[asyncio]>=2.0.0,<3.0.0
aiosqlite>=0.19.0,<1.0.0
aiofiles>=24.1.0
python-multipart>=0.0.27,<1.0.0
httpx>=0.25.0,<1.0.0
celery>=5.4.0
redis>=5.0.0,<6.0.0
slowapi>=0.1.9                       # Imported by backend/main.py + middleware/rate_limiter.py — needed for ty type-resolution in CI
sentry-sdk[fastapi]>=2.0.0           # Optional runtime dep (Sentry init in main.py / celery_worker.py); installed so ty can resolve `sentry_sdk.integrations.*` imports

# ML stack used by tests
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
pyarrow>=23.0.1
polars>=1.36.0
scikit-learn>=1.8.0,<2.0.0
xgboost>=2.1.4
lightgbm>=4.0.0
scipy>=1.13.1
imbalanced-learn>=0.14.1,<1.0.0
joblib>=1.3.0
optuna>=3.0.0                        # Imported by skyulf-core/tests/test_tuning_engine.py — needed for ty type-resolution in CI
optuna-integration>=3.0.0            # HAS_OPTUNA also requires this fallback import path to succeed (see engine.py)
cmaes>=0.10.0                        # Required by optuna's CmaEsSampler, exercised in test_tuning_engine.py
matplotlib>=3.7.0                    # Imported by skyulf-core/tests/test_profiling_visualizer.py — needed for ty type-resolution in CI
rich>=13.0.0                         # Imported by skyulf-core/tests/test_profiling_visualizer.py — needed for ty type-resolution in CI
vaderSentiment>=3.3.2                # Imported by skyulf-core/tests/test_profiling_text.py — needed for ty type-resolution in CI
shap>=0.46.0,<1.0.0                   # Imported by skyulf-core/skyulf/modeling/_explainability/shap_explanation.py — needed for ty type-resolution in CI and exercised in test_explainability.py

# Test + tooling
pytest>=7.4.0,<10.0.0
pytest-asyncio>=0.21.0,<2.0.0
pytest-cov>=4.1.0,<8.0.0
hypothesis>=6.100,<7.0.0
syrupy>=4.0.0
pytest-benchmark>=5.0.0,<6.0.0
ruff>=0.8,<1.0
lizard>=1.23.0
ty>=0.0.55,<0.0.61
pandas-stubs>=2.0.0
orjson>=3.9.0
pyscan-rs>=2.1.3                     # Dependency vulnerability scanner (OSV) for the security CI gate
