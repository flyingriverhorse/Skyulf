# SM-00 development/test lane; requires Java 17 (not installed by Python).
# Does not imply that Skyulf nodes already support Spark execution.
# Kept separate from base/backend dependencies and Databricks Runtime.
-e ./skyulf-core[spark]
pyspark==4.0.3
pytest>=9.0.0,<10.0.0
# Root pytest configuration declares asyncio_mode.
pytest-asyncio>=0.21.0,<2.0.0
pandas>=2.0.0,<3.0.0
pyarrow>=21.0.0
