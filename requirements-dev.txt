# Development requirements (linters, formatters, test runners)
ruff>=0.8,<1.0                        # Lint + format + import sort (replaces black/flake8/isort)
pytest>=7.4.0,<10.0.0
pytest-asyncio>=0.21.0,<2.0.0
pytest-cov>=4.1.0,<5.0.0
hypothesis>=6.100,<7.0.0           # Property-based tests (engine parity, ingestion fuzz)
syrupy>=4.0.0                       # Snapshot assertions — pipeline JSON regression tests
pytest-benchmark>=5.0.0,<6.0.0     # Opt-in performance benchmarks (--benchmark-only)
lizard>=1.23.0                      # Cyclomatic-complexity gate (CCN 8) — mirrors CI
factory-boy>=3.3.0,<4.0.0
faker>=19.0.0,<20.0.0
pre-commit>=3.4.0
pre-commit-hooks>=4.6.0              # Hygiene hooks run via python -m (avoids AppLocker-blocked .exe wrappers)
moto[s3]>=5.0.0,<6.0.0              # Mock AWS services for testing
safety>=3.6.1                        # Security vulnerability scanner (CLI)
pyscan-rs>=2.1.3                     # Rust-based dependency vulnerability scanner (OSV database)
types-setuptools
types-requests
types-PyYAML
pandas-stubs

# Optional helpful REPL / notebook tools for development
ipython>=8.18.1
jupyter>=1.1.1
rich>=13.0.0
