# Independent review of scanner follow-up

Reviewed the current frontend source/test diff against HEAD `5a63fe55`, including the untracked `trainingConfig.test.ts`. No blocking findings or supported-behavior regressions found. This is a bounded review, not a repository-wide security audit.

- `trainingConfig.ts:23-48`: the five CV and four tuning keys and every formatter body are preserved. `Map.get` retains the dispatch order, source selection, scalar return type, and missing-field fallback. `TrainingRows.tsx:10-13,30` supplies hardcoded field labels. Inherited names passed directly to the exported helper now return `'-'`; that is useful helper hardening, but does not establish an application exploit.
- `recordParsers.ts:23-81` and `operationalContext.ts:121-126`: the private mapped object still requires every record kind and its corresponding discriminated-union result. `Object.entries` registers its own ten parser properties only. Each parser body is unchanged. The only runtime consumer uses the union result, so losing per-key correlation on the exported Map creates no caller regression. The Map itself becomes the runtime allowlist; removing `isRecordKind` preserves rejection of unknown and inherited names. The old implementation already rejected these URL kinds through its explicit whitelist.
- `PipelineDiffView.test.tsx:52-59`: the new substring matcher still checks the parenthesized selection count for the static zero/one/three-job cases. The former regular expression interpolated only an array length in a test. This review found no untrusted regular-expression input or command-execution path.
- Regression coverage is relevant: the new direct helper tests check unknown and inherited names, and the URL tests check four inherited names. Existing comparison-table tests assert supported CV/tuning source precedence, zero/empty/null values, and strategy defaults; existing operational-context tests cover all ten kinds, numeric IDs, optional fields, malformed values, and exact public URLs.

Inspected the local ESLint complexity script and before/after outputs. It measures every function with `complexity: ['warn', 0]`: training config maximum/sum stays 8/35; PipelineDiffView tests stay 3/43; operational context stays maximum 10 and sum falls 23 to 22 by removing one helper; record parsers stay 3/31. No measured function-level increase appears. These are local ESLint measurements; the external dashboard algorithm and scanner status remain unverified.

`git diff --check -- frontend/ml-canvas/src` passed in this review. The root agent supplied the focused test evidence (baseline 107 passing / 4 new helper failures; final 111 passing across 4 files) and is running the global gates; I did not duplicate those runs or independently claim their results. No source files were edited.

Verdict: accept this scoped cleanup and helper hardening, subject to the root agent's final gates. Describe it as replacing scanner-flagged dynamic lookup/test patterns; do not describe it as a demonstrated command-injection fix or an externally confirmed clean scan.
