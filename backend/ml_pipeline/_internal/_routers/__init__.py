"""Sub-routers extracted from the historic monolithic `api.py`.

Each module exposes a `router` (FastAPI APIRouter) that is included
by `backend.ml_pipeline.api` so the public URL surface is unchanged.
"""
