"""Pipeline execution layer: turns a saved canvas graph into a skyulf-core run.

Holds the Celery task entry points, the execution engine and its node runners,
the job services and strategies, the artifact stores, the model registry and
the deployment/serving path.
"""
