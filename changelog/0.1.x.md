# Changelog — 0.1.x Series

For the version index, see root [CHANGELOG.md](../CHANGELOG.md).

---

## v0.1.19 — Theme-Aware Charts

### 🎨 Dark Mode
- Theme-aware `getChartTheme()`/`isDarkMode()` helpers for consistent axis, grid, text, and background colors.
- Fixed correlation matrix, distribution charts, scatter plots, box plots, and tooltips to use theme-aware colors instead of hardcoded values.

### 🔄 Download UX
- Download buttons cycle through Download → spinning → green checkmark states; disabled during export.

### 🧹 Code Quality
- Deduplicated color arrays into shared `COLORS` constant in `constants.ts`.

---

## v0.1.18 — Lint Zero & EDA Polish

### 🧹 Code Quality
- Resolved all 59 ESLint issues. Added ESLint + build checklist to CI instructions.

### 🐛 Bug Fixes
- Fixed mypy `union-attr` error in `data_ingestion/router.py`.

### 🎨 EDA
- Null guards + `EmptyState` on 5 EDA tabs. Mobile hover buttons and dropdown overflow fixes.
- CausalTab download via `html-to-image`. Dark mode tooltips via shared `getTooltipContentStyle()`.
- Migrated from `html2canvas` to `html-to-image` project-wide for proper SVG exports.

---

## v0.1.17 — SQL Security & Resilience

### 🔒 Security
- SQL injection protection: regex-validated table names, SELECT-only queries enforced in `DatabaseConnector`.

### ⚡ Performance
- SQLAlchemy connection pooling with `QueuePool` (pool_size=5, recycle=30 min). Dialect-aware query timeouts (30 s).

### 🎨 Frontend
- Shared `LoadingState`/`ErrorState`/`EmptyState` components. CSV download per dataset row.
- DataSources default filter to "All" with "Processing" tab. Download buttons fixed for older uploads.

### ♿ Accessibility
- `aria-label` on all icon-only buttons. Modal accessibility (`role="dialog"`, `aria-modal`, Escape/backdrop dismiss).
- `aria-current="page"` on active nav links. Sidebar search labeled.

### 🎨 EDA
- Inline expanded stats (zeros, negatives, VIF, rare labels, skewness/kurtosis). Removed redundant `VariableDetailModal`. Adaptive distribution chart height.

### 🔧 Backend
- `GET /data/api/sources/{source_id}/export` endpoint (CSV, Parquet, 1–50k rows).

### 🧪 Tests
- 31 tests for `DatabaseConnector` (table validation, query validation, pooling, integration).

---

## v0.1.16 — Security Hardening

### 🔒 Security
- `AUTH_FALLBACK_ENABLED` default → `False`. Path traversal protection in `FileSystemCatalog` and `LocalArtifactStore`.
- Pickle deserialization warnings in artifact stores. Error messages sanitized (full errors logged server-side only).

### 🐛 Bug Fixes
- SPA routing: catch-all `/{full_path:path}` serves `index.html` for client-side routes.
- Fixed sklearn config leak (`transform_output="pandas"`) affecting `PolynomialFeatures`/`KNNImputer` across tests.
- Added `hasattr(transformed, "values")` guards for sklearn output resilience. 2 xfailed tests now pass.

### 🧹 Cleanup
- Deleted `backend/legacy_config.py` (597 lines of dead code). Dependency audit: optional-dependency groups, core trimmed ~60→~30.
- 20 `print()` → `logger`. 20+ bare excepts now logged.

### ⚙️ CI/CD
- Frontend build step in CI (`npm ci`, `npm run lint`, `npm run build`).

### 🧪 Tests
- 14 path traversal tests for `LocalArtifactStore` and `FileSystemCatalog`.

---

## v0.1.15 — Developer Experience

### 🚀 DX
- `start.bat`/`start.sh` one-click scripts (auto-venv, .env generation, launch). Python detection, version guard, internet check.
- Multi-stage Dockerfile. Docker Compose fix (removed `profiles: [dev]`). `.env.example` defaults `USE_CELERY=false`. Makefile.

### 🐛 Bug Fixes
- Removed strict upper bounds on `python-multipart`, `numpy`. Pinned `aiohttp` to prevent 40-version backtracking.
- Start script retries connectivity 5×.

### 🧹 Dependencies
- 15 unused deps removed. Geo/auth/viz/NLP moved to optional extras.

### ⚙️ Config
- Replaced monolithic `config.py` (597 lines) with `backend/config/` package: 10 domain mixins.
- Zero Pydantic shadow warnings. All 32 imports backward compatible.

---

## v0.1.14 — Cross-Validation & Experiments

### 🧪 CV
- Nested CV across full stack. Shuffle Split option in Advanced Tuning UI. Time Series CV with auto-sort by datetime.
- Date column selector in Basic/Advanced Training when TS split chosen. Fixed `aggregated_metrics` extraction.

### 🐛 Bug Fixes
- Advanced Tuning CV metrics now populated via `cross_validate()` on best model.
- CV params read from `tuning_params` (not `node.params`). CV std uses sample std (`ddof=1`), 6 decimal precision.
- Nested CV post-tuning uses stratified/k-fold instead of re-running nested CV.

### 🎨 Frontend
- CV metrics toggle on Experiments charts. Purple accent for CV metrics in JobsDrawer.
- Unified training config in comparison table. Hyperparameters parity across job types.

### 🔧 Backend
- `config` field on `JobInfo` for advanced tuning jobs.

### 📚 Docs
- Cross-Validation guide, expanded installation/configuration/troubleshooting pages, FAQ, Platform Walkthrough, Platform Setup.
- Landing page rewrite with feature table, install commands. Footer links fixed. Smart quotes replaced.

---

## v0.1.13 — Advanced Tuning Strategies

### 🧠 Core & Backend
- Dynamic `strategy_params` in `TuningConfig`. Optuna sampler/pruner configuration. Halving `factor`/`min_resources`.

### 🎨 Frontend
- `StrategySettingsModal` with per-strategy fields. Educational tooltips. Payload integration in `pipelineConverter.ts`.

---

## v0.1.12 — Code Quality & Test Coverage

### 🐛 Bug Fixes
- Fixed `engine.__name__` → `engine.name` in `StandardScalerApplier`. Bare `except:` → `except Exception:`.
- `APP_VERSION` updated to `0.1.12`.

### 🔧 DevOps
- CI triggers on push/PR. Fixed dependency pins (`polars`, `aiofiles`, `optuna`). Version alignment across components.

### ✅ Tests
- 41 preprocessing tests, 22 modeling tests. skyulf-core ~8→71 tests.
- F821 lint fix, bare except fix, flake8 `--exit-zero`. mypy zero errors across 162 source files.
- `SplitDataset(DataFrame, None)` bug fix. Pipeline and strategy test fixes. 268 tests passing.

### 🛡️ Encoding
- Target-column guard: OneHot/Dummy/Hash/TargetEncoder auto-exclude target column. 12 guard tests.
- `extract_target_label_encoder()` searches by target column name + `__target__`. 9 decode tests.

### 🔧 CI
- mypy stubs for optional deps. Lazy `pyarrow` import. `skipif` for parquet tests without pyarrow.
- `KBinsDiscretizer` `quantile_method` fix. `pyarrow` in CI requirements.

---

## v0.1.11 — EDA-First Packaging & Drift

- Drift 500 fix: safe type casting in `DriftCalculator`. Reference data saved at Data Loading/Splitting (raw), not Training.
- Optional XGBoost/modeling imports handled gracefully. Package extras: `eda`, `modeling-xgboost`.
- Packaging metadata alignment. Reduced base `skyulf-core` deps (tuning/imbalanced → optional extras).

---

## v0.1.10 — Model Expansion (15 new models)

### 🧠 Models
- Classification: SVC, KNeighbors, DecisionTree, GradientBoosting, AdaBoost, XGBoost, GaussianNB.
- Regression: Linear, Lasso, ElasticNet, SVR, KNeighbors, DecisionTree, GradientBoosting, AdaBoost, XGBoost.
- Hyperparameter definitions for all new models.

### 🎨 Frontend
- "Requires Scaling" hint for SVM/KNN/Ridge/Lasso. `@node_meta` tags. Collapsible Experiments sidebar (320px).

### 🐛 Bug Fixes
- Regression charts: target label decoding no longer corrupts float `y_true`/`y_pred`.
- Y-axis label centering. Sidebar transition animations removed. Download Graph on evaluation charts.

---

## v0.1.9 — Registry & Polars Compatibility

### 🏗️ Architecture
- Self-registering node system via `@node_meta`. Backend auto-discovers nodes at runtime.

### 🧹 Polars
- Full Polars support for all preprocessing nodes. Hybrid execution for sklearn deps (SMOTE).

### 🎨 Frontend
- `createModelingNode` factory. Tabs renamed "Basic Training" / "Advanced Tuning".

### 🧪 Quality
- 50+ node parity tests. `docs/guides/engine_mechanics.md`.

---

## v0.1.8 — Unsupervised & Monitoring

### 🧠 Analysis
- K-Means clustering in EDA. PCA loadings (2D/3D). Decision Tree rules for regression. VIF + sentiment analysis.

### 📊 Drift
- `DriftCalculator` (Wasserstein, KS, PSI, KL). Reference snapshot persistence. Drift dashboard with histograms. Schema drift alerts.

### 🔧 Backend
- Strategy Pattern for task execution. Deduplicated PCA/Clustering data prep. Renamed job types.

### 🎨 Frontend
- Consolidated EDA (Clustering + PCA). `EDAVisualizer` parity with Web UI. Task type overrides.

---

## v0.1.7 — Advanced EDA & Profiling

### 🧠 Analysis
- Decision Tree rule extraction (Purity, Fidelity). PC Algorithm causal discovery. Outlier explanations. Normality + stationarity tests.

### 📊 Profiling
- Feature importance chart. Interactive cross-filtering. 3D scatter plots. Geo/time series detection. Target analysis.

### 🚀 Performance
- Polars profiling engine. Dynamic sampling for PCA/correlation. Rich terminal `EDAVisualizer`.

### 🎨 Frontend
- Full-width layout fix. Jobs page with status and logs.

---

## v0.1.6 — Polars Migration

### 🚀 Core
- `SkyulfDataFrame` Protocol + `EngineRegistry` for Pandas/Polars dual-backend. ~4x speedup on large datasets.
- `SklearnBridge` for Polars→Numpy conversion.

### 🐛 Bug Fixes
- `TargetEncoder` fit without explicit `y`. `LabelEncoder`/`PolynomialFeatures` Polars robustness.

### 📚 Docs
- Titanic leakage-proof notebook. Performance benchmarks.

---

## v0.1.5 — Registry & Catalog Architecture

### 🔧 Backend
- `DataCatalog` + `S3Catalog`/`S3Connector` for S3 integration. Hybrid artifact storage (S3/local). Self-registering nodes.

### 🧹 Refactoring
- `ArtifactFactory`, `JobService`, `EvaluationService`. Logging config moved. `registry.py` → `node_definitions.py`.

### 🐛 Bug Fixes
- S3 artifact listing, inference, evaluation fixes. Schema detection for S3 models. 100% test pass (113 tests).

### 🎨 Frontend
- S3 UI in Add Source. Artifact browser with storage type. Leakage warnings, modal responsiveness.

---

## v0.1.4 — Polynomial, Security & API

### 🎨 Frontend
- Standalone `PolynomialFeatures` node. `uuid` v4 for Node/Pipeline IDs. Leakage warnings in graph store.

### 🔧 Backend
- `scikit-learn` pinned `<1.6.0`. 82 mypy errors resolved. Zero flake8 errors.
- `log_callback` for Tuner/Models. CI Redis service. `dataset_id` fallback for `data_loader`. Pandas 2.x `ValueReplacement` fix.

### 🧠 Logic
- `PolynomialFeatures` duplicate-column fix. Pandas output compatibility fix.
- Router restructuring under `/api/ml`. Live logs propagation fix.

---

## v0.1.3 — Code Quality & Stability

### 🛡️ Backend
- Tuning artifact tuple unwrapping fix. Label-decoded predictions in deployment. Evaluation label decoding.
- Deterministic artifact bundling. Composite pipeline bundling. DB variable shadowing/MySQL/Snowflake fixes.

### 🧠 Engine
- Feature-engineering artifact key resolution consolidated. Malformed f-string logging fix.

### 🧹 Frontend
- Non-null assertions → runtime checks. Explicit void returns. `Reflect.deleteProperty`.
- ROC curve type normalization. Readable class selector. Confusion matrix clarity.

---

## v0.1.2 — Tuning & Versioning Consistency

### 🎯 Tuning
- `TuningCalculator` auto-refits best model on full data. `StatefulEstimator` for tuning jobs. Full metrics for tuning jobs.

### 🔢 Versioning
- Unified version counter across Training/Tuning jobs.

### 📦 Distribution
- `skyulf-core` 0.1.2 published to PyPI.

---

## v0.1.1 — Observability & Stability

### 🔍 Logging
- Custom tuning loop with real-time candidate/fold logging. Optuna trial logging. Exact model params logged.
- CV progress streamed fold-by-fold. 108/108 tests passing.

### 🏗️ Architecture
- Core ML logic consolidated in `skyulf-core/`. Tuning logic migrated to `skyulf-core/skyulf/modeling/tuning/`.
- Clean `NodeRegistry` vs `AdvisorEngine` separation.

---

## v0.1.0 — Foundation & Deployment

### 🚀 Core
- Polars for high-performance ingestion. Calculator/Applier pattern for stateless transforms.
- Hybrid async engine (Celery or background threads) with job cancellation.

### 🎨 Frontend
- React + Vite SPA. Dashboard with activity feed. Detailed data preview. UI consistency pass.

### 🧠 Training
- Standard Training + Advanced Tuning (Grid/Random/Halving). Experiment tracking with side-by-side comparison. Confusion matrices, ROC, feature importance.

### 📦 MLOps
- Model Registry with versioning. One-click deployment with inference API. Unified Jobs Drawer.
