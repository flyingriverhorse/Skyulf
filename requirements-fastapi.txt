# FastAPI MLops Application Requirements
# This file contains dependencies specific to the FastAPI implementation

# === LOCAL MONOREPO PACKAGES ===
# Provides the `skyulf` Python package (core preprocessing/modeling/pipeline engine)
-e ./skyulf-core

# === CORE FASTAPI DEPENDENCIES ===
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
pydantic>=2.4.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0        # Environment variable loading
python-multipart>=0.0.6             # Form data / file upload support for FastAPI
orjson>=3.9.0                       # High performance JSON serialization

# === ASYNC TASK QUEUE ===
celery[redis]>=5.4.0,<6.0.0
redis>=5.0.0,<6.0.0

# === CAUSAL INFERENCE ===
causal-learn>=0.1.3.0

# === ASYNC DATABASE SUPPORT ===
sqlalchemy[asyncio]>=2.0.0,<3.0.0
aiosqlite>=0.19.0,<1.0.0            # Async SQLite support
asyncpg>=0.29.0,<1.0.0              # Async PostgreSQL support (optional)

# === DATA STORAGE & FORMATS ===
pyarrow>=21.0.0                      # Parquet support for Pandas/Polars
boto3>=1.34.0                        # AWS SDK (transitive via s3fs, pinned for stability)
s3fs>=2023.12.0                      # S3 Filesystem for Pandas
aiohttp>=3.9.0,<4.0.0               # Pinned to avoid pip backtracking (transitive via s3fs)
aiofiles>=24.1.0                     # Async file operations

# === STATISTICS & ANALYSIS ===
scipy>=1.10.0,<2.0.0
statsmodels>=0.14.0,<1.0.0
patsy>=0.5.0,<1.0.0                  # Required by statsmodels

# === HTTP CLIENT (ASYNC) ===
httpx>=0.25.0,<1.0.0

# === TERMINAL & LOGGING ===
rich>=13.0.0                         # Beautiful terminal logging

# === DATA PROCESSING ===
polars>=1.36.0                       # Fast DataFrame library (V2 Ingestion)
xlsx2csv>=0.8.0                      # Excel to CSV converter (used by Polars internally)
openpyxl>=3.1.0                      # Excel reading support (used by Pandas internally)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0
scikit-learn>=1.8.0,<2.0.0
xgboost>=2.1.4                       # XGBoost classifier/regressor models
optuna>=4.0,<5                       # Bayesian/early-stopping hyperparameter search
optuna-integration>=4.0,<5           # OptunaSearchCV for sklearn hyperparameter tuning
cmaes>=0.10                          # Required by optuna's CmaEsSampler (not auto-installed)
imbalanced-learn>=0.14.1,<1.0.0     # Sampling strategies for imbalanced datasets
rapidfuzz>=3.6.1,<4.0.0             # String similarity metrics for feature engineering

# === NLP ANALYTICS ===
vaderSentiment>=3.3.2,<4.0.0        # Sentiment scoring utilities

# === OPTIONAL EXTRAS (install separately if needed) ===
# Geospatial:  pip install -r requirements-geo.txt
# Dev tools:   pip install -r requirements-dev.txt
